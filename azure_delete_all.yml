---
- name: Playbook to delete all VM's of the current user
  hosts: localhost
  connection: local
  gather_facts: no
  vars:
    group: "user_{{ tower_user_name }}"
    vms: "{{ groups[group] }}"

  tasks:
  - name: show inventory 2
    debug:
      msg: "current inventory: {{ vms }}"
